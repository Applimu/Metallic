
def fun Int (fun Int (fun Int Int)) : fib_helper :=
	fn Int: a do  
	fn Int: b do
	fn Int: i do
	(fn Bool: i_is_zero do
		match i_is_zero
		case true do b
		case false do fib_helper b (add a b) (add i -1)
		end
	) (eq i 0)


def fun Int Int : fib := fib_helper 0 1

eval fib 50
